# 后端 API 基础地址
# 开发时使用本地 FastAPI 后端
VITE_API_BASE_URL=http://127.0.0.1:8000

# 默认关闭 Mock，调用真实后端
VITE_USE_MOCK=false

# 账号模式必须与后端 AUTH_ENABLED 同步；默认保持关闭。
# 开启后，页面会使用 HttpOnly Cookie 恢复会话并保护生成与历史入口。
VITE_AUTH_ENABLED=false
